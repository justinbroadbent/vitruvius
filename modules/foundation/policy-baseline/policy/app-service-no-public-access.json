{
  "displayName": "App Service apps must disable public network access",
  "description": "Audits or denies any App Service whose public network access is not Disabled. Internal-facing apps must be reached over a private endpoint, not the public internet. Ships Audit-first per ADR 0008; promote to Deny once Audit-mode evidence supports it. Supports GLBA Safeguards access control.",
  "mode": "Indexed",
  "policyRule": {
    "if": {
      "allOf": [
        { "field": "type", "equals": "Microsoft.Web/sites" },
        { "field": "Microsoft.Web/sites/publicNetworkAccess", "notEquals": "Disabled" }
      ]
    },
    "then": {
      "effect": "[parameters('effect')]"
    }
  },
  "parameters": {
    "effect": {
      "type": "String",
      "metadata": { "displayName": "Effect", "description": "Audit, Deny, or Disabled." },
      "allowedValues": ["Audit", "Deny", "Disabled"],
      "defaultValue": "Audit"
    }
  }
}
