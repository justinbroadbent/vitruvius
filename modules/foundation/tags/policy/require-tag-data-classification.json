{
  "displayName": "Require 'data-classification' tag on resources",
  "description": "Validates that the 'data-classification' tag is present. Per ADR 0010, classification drives customer-managed-key, private-endpoint, and retention defaults downstream.",
  "mode": "Indexed",
  "policyRule": {
    "if": {
      "field": "tags['data-classification']",
      "exists": "false"
    },
    "then": {
      "effect": "[parameters('effect')]"
    }
  },
  "parameters": {
    "effect": {
      "type": "String",
      "metadata": {
        "displayName": "Effect",
        "description": "Audit, Deny, or Disabled. Defaults to Audit per ADR 0008."
      },
      "allowedValues": ["Audit", "Deny", "Disabled"],
      "defaultValue": "Audit"
    }
  }
}
