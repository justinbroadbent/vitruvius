{
  "displayName": "Require 'business-criticality' tag on resources",
  "description": "Validates that the 'business-criticality' tag is present. Per ADR 0010, business-criticality drives SLA monitoring, geo-redundancy, and PIM-only change-path defaults downstream.",
  "mode": "Indexed",
  "policyRule": {
    "if": {
      "field": "tags['business-criticality']",
      "exists": "false"
    },
    "then": {
      "effect": "[parameters('effect')]"
    }
  },
  "parameters": {
    "effect": {
      "type": "String",
      "metadata": {
        "displayName": "Effect",
        "description": "Audit, Deny, or Disabled. Defaults to Audit per ADR 0008."
      },
      "allowedValues": ["Audit", "Deny", "Disabled"],
      "defaultValue": "Audit"
    }
  }
}
